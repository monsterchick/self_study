pwd
# prints current path

cp
# copy file

rm -rf folderName
# removes folder with files inside

touch fileName
# creates a file

mkdir folderName
#creates a folder

cd /
# move to root directory
cd -
# move to the previous directory
cd ..
# move up one directory level
cd ~
# move from a higher level directory to a specific sub-directory 
